export const component: DraymanComponent = async ({ forceUpdate, ComponentInstance }) => {

    while (true) {
    }

    return () => {
        return (
            <div>Hello, World!</div>
        )
    }
}